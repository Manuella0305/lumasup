name: Sync Opportunités

on:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Call sync API
        run: |
          curl -X POST "https://lumasup.vercel.app/api/sync-opportunites?secret=${{ secrets.CRON_SECRET }}"
